sys: "cjsc"

logging:
  level: "info"

webif:
  debug: false
  http_port: 8000
  redirect_subpath: "/"
  binary_json: True
  binary_key: "58ae9756116f78430e370c5c25519f63"
  postgresql:
    host: "postgres"
    port: 5432
    user: "postgres"
    password: "Eimah0Sielah0xi8Sh"
    db_name: "user_proxy"
    check_interval: 60
  session:
    host: "redis"
    port: 6379
    db: 3
    password: 'Eimah0Sielah0xi8Sh'
    expire_seconds: 86400
  sqlalchemy:
    echo: False

unify_auth:
  auto_create_user: True
  show_role_manage: True  # 是否展示角色管理界面
  permission:
    autodoc_overall:
      admin: "管理员"
      normal: "普通用户"
    calliper:
      admin: "管理员"
      normal: "普通用户"
  customer_permission:
    super_admin:
      autodoc_overall:
        - admin
    cooperator:
      autodoc_overall:
        - normal
  auth_self:
    app_id: "user_proxy"
    secret_key: "I-do-not-know"
    token_expire: 120
  auth_config:
    auth_autodoc_overall:
      app_id: "autodoc"
      secret_key: "show-me-the-money"
      token_expire: 120
      host: ""
      subpath: "autodoc/"
      auth_api: "api/v1/user/unify-login"
      logout_api: "api/v1/logout"
      department_url: "http://172.16.121.75/autodoc/api/v1/cjsc/department/sync"
    auth_calliper:
      app_id: "calliper"
      secret_key: "aim4AhngiiHeexee"
      token_expire: 120
      host: "http://calliper"
      subpath: ""
      auth_api: "api/v1/user/unify-login"
      logout_api: "api/v1/logout"

front_config:
  only_customer_permission: True  # 角色权限仅包含客户自定义权限环境
  auto_redirect_sub_sys: True # 只有一个子系统，普通用户是否直接跳转到子系统
  internal: False  # 标记内部环境，用于区分内外环境登录、退出等差异
  show_user_allow_login: True  # 用户管理里展示用户状态列
  customer_login_api: '/user/cas-login' # 未登录直接访问trident主页，跳转至客户认证界面接口
  pd_env: false  # 未登录直接访问trident /#/login页，跳转至客户认证界面接口
  allow_change_password: True  # 修改密码
  cjsc_business_segment: # 长江证券业务板块
    - "保荐业务"
    - "债券业务"
    - "新三板业务"
    - "ABS业务"
  customer_role_permission_contrast:
    super_admin: '业务管理员'
    cooperator: '内控人员'
  sub_sys_config:
    overall:
      open: true
      title: '全面复核'
    calliper:
      open: true
      title: '文档智能比对'
      captions: "自动比对不同版本文档的差异变更信息，以可视化形式展示差异、并支持比对结果导出。"
 
cas_auth:
  server: "https://castest.cjsctest.com.cn" # 正式环境："https://castest.cjsc.com.cn"
  cas_token_session_key: "ticket"
  cas_username_session_key: "CAS_USERNAME"
  cas_attributes_session_key: "CAS_ATTRIBUTES"
  cas_after_login: ""
  cas_after_logout: "/api/v1/user/cas-login"
  login_uri: "/cas/login"
  logout_uri: "/cas/logout"
  validate_uri: "/cas/serviceValidate"
  system_name: ''
  sys_admin: "admin"

customer_uri_info:
  debug: false
  base_server: "http://172.16.126.36:8081"
  get_project_type_uri: "/pmdev/sys/webservice/sysSynchroPaoDingWebService?wsdl"  # 获取业务类型接口
  sync_dept_user_uri: "/pmdev/sys/webservice/sysSynchroGetPMOrgWebService?wsdl"  # 同步部门和用户信息接口

worker:
  app_name: "trident"
  broker: "redis://:Eimah0Sielah0xi8Sh@redis:6379/2"
  enable: true
  period_tasks:
    sync_dept_user_info_task:
      minute: 0
      hour: 0