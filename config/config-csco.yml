sys: "csco"

logging:
  level: "info"

webif:
  debug: false
  http_port: 8000
  redirect_subpath: ""
  postgresql:
    host: "postgres"
    port: 5432
    user: "postgres"
    password: "bqlojUu7TaEKM8GNyu"
    db_name: "user_proxy"
    check_interval: 60
  session:
    host: "redis"
    port: 6379
    db: 3
    password: 'bqlojUu7TaEKM8GNyu'
    expire_seconds: 86400
  sqlalchemy:
    echo: False
  feature:
    check_captcha: true


unify_auth:
  auto_create_user: True
  permission:
    grater:
      normal: "普通用户"
      admin: "管理员"
  auth_self:
    app_id: "user_proxy"
    secret_key: "I-do-not-know"
    token_expire: 120
  auth_config:
    auth_grater:
      app_id: "bankstatement"
      secret_key: "hello_bankstatement"
      token_expire: 120
      host: ""
      subpath: "grater/"
      auth_api: "api/v2/user/unify-login"
      logout_api: "api/v2/logout"

cas_auth:
  server: "https:// 172.16.96.155"
  cas_token_session_key: "ticket"
  cas_username_session_key: "CAS_USERNAME"
  cas_attributes_session_key: "CAS_ATTRIBUTES"
  cas_after_login: ""
  cas_after_logout: "/api/v1/user/cas-login"
  login_uri: "/sso/login"
  logout_uri: "/sso/logout"
  validate_uri: "/sso/proxyValidate"
  trident_url: ""  # trident服务地址
  appid:

front_config:
  auto_redirect_sub_sys: False # 只有一个子系统，普通用户是否直接跳转到子系统
  user_manage: True # 展示用户管理界面
  customer_login_api: '/user/cas-login' # 未登录直接访问trident主页，跳转至客户认证界面接口
  pd_env: false  # 未登录直接访问trident /#/login页，跳转至客户认证界面接口
  sub_sys_config:
    grater:
      open: true
      title: '银行流水识别'
