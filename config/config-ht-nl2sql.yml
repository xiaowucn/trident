sys: "ht"

logging:
  level: "info"

webif:
  debug: False
  http_port: 8000
  postgresql:
    host: "postgres"
    port: 5432
    user: "postgres"
    password: "VrDJrh0KN109"
    db_name: "trident"
    check_interval: 60
  sqlalchemy:
    echo: False
unify_auth:
  auth_self:
    app_id: "user_proxy"
    secret_key: "I-do-not-know"
    token_expire: 120
  auth_config:
    auth_nl2sql:
      app_id: "nl2sqlengine"
      secret_key: "hello_nl2sqlengine"
      token_expire: 120
      host: "http://nl2sql"
      subpath: ""
      auth_api: "api/v1/user/unify-login"
      logout_api: "api/v1/logout"

ldap:
  uri: ldap://10.188.105.222:389
  search_base: o=GS001,dc=htsec,dc=net
  filter: (&(objectClass=DFStaff)(uid=%s))
  attrlist: ["o", "unitdept", "cn"]
  admin_dn: uid=ldapuser,o=GS001,dc=htsec,dc=net
  admin_password: htzqTds
