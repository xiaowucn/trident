sys: "gjzq"

logging:
  level: "info"

webif:
  debug: false
  http_port: 8000
  redirect_subpath: ""
  postgresql:
    host: "postgres"
    port: 5432
    user: "postgres"
    password: "fohki4hiequai2io0C"
    db_name: "user_proxy"
    check_interval: 60
  session:
    host: "redis"
    port: 6379
    db: 3
    password: 'fohki4hiequai2io0C'
    expire_seconds: 86400
  sqlalchemy:
    echo: False
  feature:
    check_captcha: true


unify_auth:
  auto_create_user: True
  permission:
    autodoc_overall:
      admin: "管理员"
      normal: "普通用户"
    grater:
      normal: "普通用户"
      admin: "管理员"
  auth_self:
    app_id: "user_proxy"
    secret_key: "I-do-not-know"
    token_expire: 120
  auth_config:
    auth_autodoc_overall:
      app_id: "autodoc"
      secret_key: "show-me-the-money"
      token_expire: 120
      host: ""
      subpath: "autodoc/"
      auth_api: "api/v1/user/unify-login"
      logout_api: "api/v1/logout"
    auth_grater:
      app_id: "bankstatement"
      secret_key: "hello_bankstatement"
      token_expire: 120
      host: ""
      subpath: "grater/"
      auth_api: "api/v2/user/unify-login"
      logout_api: "api/v2/logout"

oauth2_auth:
  client_id: "UsXm6E0LGF"
  client_secret: "ed4402dd-512c-483a-b210-8b263825738c"
  base_url: "http://sso.gjzqtest.cn/"
  authorize_api: "profile/oauth2/authorize"
  access_token_api: "profile/oauth2/accessToken"
  user_info_api: "profile/oauth2/profile"
  request_access_token: True  # 根据code获取access_token时，需后端请求方式，非客户回调单点接口

use_customer_logout: True
oa_auth_url: "https://sso.gjzq.cn/esc-sso/logout"

front_config:
  auto_redirect_sub_sys: False # 只有一个子系统，普通用户是否直接跳转到子系统
  user_manage: True # 展示用户管理界面
  customer_login_api: '/user/oauth-login' # 未登录直接访问trident主页，跳转至客户认证界面接口
  pd_env: true  # 未登录直接访问trident /#/login页，跳转至客户认证界面接口
  sub_sys_config:
    overall:
      open: true
      title: '全面复核'
    grater:
      open: true
      title: '银行流水识别'


