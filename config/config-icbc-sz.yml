sys: "icbc-sz"

logging:
  level: "debug"

webif:
  debug: False
  http_port: 8000
  redirect_subpath: "ibps/trident/"
  postgresql:
    host: "postgres"
    port: 5432
    user: "postgres"
    password: "V4gy42paHQddd7Mc"
    db_name: "user_proxy"
    check_interval: 60
  session:
    host: "redis"
    port: 6379
    db: 1
    password: "twTPTEuU9SQ7brTr"
    expire_seconds: 86400
  sqlalchemy:
    echo: False

unify_auth:
  auto_create_user: True
  permission:
    glazer:
      normal: "普通用户"
      admin: "管理员"
  auth_self:
    app_id: "user_proxy"
    secret_key: "I-do-not-know"
    token_expire: 120
  auth_config:
    auth_glazer:
      app_id: "glazer"
      secret_key: "hello_glazer"
      token_expire: 120
      host: ""
      subpath: "glazer/"
      auth_api: "api/v1/user/unify-login"
      logout_api: "api/v1/logout"

front_config:
  allow_change_password: True  # 修改密码
  user_manage: True # 展示用户管理界面
  auto_redirect_sub_sys: False # 只有一个子系统，普通用户是否直接跳转到子系统
  internal: False  # 标记内部环境，用于区分内外环境登录、退出等差异
  sub_sys_config:
    glazer:
      open: true
      title: "文档智能撰写"
      captions: "支持各类制式文档的智能撰写，并支持在线修改数据，导出生成后的文档。"
